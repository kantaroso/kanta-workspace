apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-php
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: slow
